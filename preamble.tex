\documentclass[12pt,letterpaper]{article}

% ─── Fuente Arial (Helvetica) ─────────────────────────────────────────────────
\usepackage[scaled]{helvet}
\renewcommand{\familydefault}{\sfdefault}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% ─── Idioma ───────────────────────────────────────────────────────────────────
\usepackage[spanish,es-nodecimaldot]{babel}

% ─── Matemáticas ──────────────────────────────────────────────────────────────
\usepackage{amsmath,amssymb}

% ─── Layout ───────────────────────────────────────────────────────────────────
\usepackage{geometry}
\geometry{top=2.2cm, bottom=2.2cm, left=2.5cm, right=2.5cm}

% ─── Tablas ───────────────────────────────────────────────────────────────────
\usepackage{booktabs,array,colortbl}

% ─── Gráficos ─────────────────────────────────────────────────────────────────
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{tikz}
\usepackage{float}
\usepackage{graphicx}
\usepackage{pdfpages}

% ─── tcolorbox ────────────────────────────────────────────────────────────────
\usepackage[most]{tcolorbox}

% ─── Encabezado ───────────────────────────────────────────────────────────────
\usepackage{fancyhdr}
\usepackage{titlesec}

% ─── Colores (paleta azul/verde, sin rojos) ───────────────────────────────────
\definecolor{azulprin}{RGB}{10,80,150}
\definecolor{azulmedio}{RGB}{30,110,190}
\definecolor{azulclaro}{RGB}{220,235,252}
\definecolor{azulborde}{RGB}{60,130,200}
\definecolor{verdeprin}{RGB}{20,120,80}
\definecolor{verdeclaro}{RGB}{215,245,228}
\definecolor{verdeborde}{RGB}{40,150,100}
\definecolor{ambar}{RGB}{180,130,0}
\definecolor{ambarcl}{RGB}{255,248,210}
\definecolor{grisclaro}{RGB}{245,246,248}
\definecolor{grismedio}{RGB}{100,110,120}

% ─── Estilos tcolorbox ────────────────────────────────────────────────────────
\tcbset{
  resultadoStyle/.style={
    enhanced, breakable,
    colback=azulclaro,
    colframe=azulborde,
    fonttitle=\bfseries\small,
    coltitle=white,
    attach boxed title to top left={yshift=-2mm, xshift=6mm},
    boxed title style={colback=azulborde, colframe=azulborde, rounded corners},
    arc=4pt, boxrule=1.2pt,
    left=6pt, right=6pt, top=6pt, bottom=6pt,
  },
  interpretacionStyle/.style={
    enhanced, breakable,
    colback=verdeclaro,
    colframe=verdeborde,
    fonttitle=\bfseries\small,
    coltitle=white,
    attach boxed title to top left={yshift=-2mm, xshift=6mm},
    boxed title style={colback=verdeprin, colframe=verdeprin, rounded corners},
    arc=4pt, boxrule=1.2pt,
    left=6pt, right=6pt, top=6pt, bottom=6pt,
  },
  pasoStyle/.style={
    enhanced,
    colback=grisclaro,
    colframe=grismedio,
    arc=3pt, boxrule=0.6pt,
    left=5pt, right=5pt, top=3pt, bottom=3pt,
  },
  tituloStyle/.style={
    enhanced,
    colback=azulprin,
    colframe=azulprin,
    colupper=white,
    fontupper=\bfseries\large,
    arc=5pt, boxrule=0pt,
    left=10pt, right=10pt, top=8pt, bottom=8pt,
  },
}

% ─── Comandos de caja ─────────────────────────────────────────────────────────
\newcommand{\resultado}[2][Resultado]{%
  \begin{tcolorbox}[resultadoStyle, title={#1}]#2\end{tcolorbox}}

\newcommand{\interpretacion}[2][Interpretaci\'on]{%
  \begin{tcolorbox}[interpretacionStyle, title={#1}]#2\end{tcolorbox}}

\newcommand{\paso}[1]{%
  \vspace{4pt}%
  \begin{tcolorbox}[pasoStyle]%
    \textbf{\color{azulprin}Paso #1:}%
  \end{tcolorbox}%
  \vspace{2pt}}

\newcommand{\formula}[1]{\vspace{2pt}\[ #1 \]\vspace{-4pt}}

% ─── Carpeta de imágenes ──────────────────────────────────────────────────────
\graphicspath{{imagenes/}}

% ─── Encabezado / pie ─────────────────────────────────────────────────────────
\setlength{\headheight}{15pt}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{azulprin}\textbf{IPN -- ESIME Culhuac\'an}}
\fancyhead[R]{\small\color{azulprin}Calidad en Ingenier\'ia}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{1.5pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{azulprin}\leaders\hrule height \headrulewidth\hfill}}

% ─── Títulos ──────────────────────────────────────────────────────────────────
\titleformat{\section}{\large\bfseries\color{azulprin}}{}{0em}{}[\color{azulborde}\titlerule]
\titleformat{\subsection}{\normalsize\bfseries\color{azulmedio}}{}{0em}{}
\titleformat{\subsubsection}{\normalsize\bfseries\color{grismedio}}{}{0em}{}
